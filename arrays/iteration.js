const users = ["Anton", "Maria", "Peter"];

for (let i = 0; i < users.length; i++) {
  console.log(`Index ${i}`, users[i]);
}

// for (const user of users) {    //если нужен объект, а не порядковый номер в массиве
//     console.log(user)
// }
